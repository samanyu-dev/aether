# Aether Environment Variables
# ----------------------------------------

# WebSocket backend endpoint for real-time agent trace streaming.
# In fully static serverless deployments (Vercel, HF Spaces), leave this blank/commented
# to automatically enable high-fidelity client-only local replay mode.
NEXT_PUBLIC_AETHER_WS_URL=ws://localhost:8000
